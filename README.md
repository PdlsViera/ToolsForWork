# ToolsForWork

This program renames PDF files using the employee's name found in the file content, removing the CPF when it appears alongside the name. It is a useful tool when there are multiple files to rename, especially if the file generator uses generic names.
